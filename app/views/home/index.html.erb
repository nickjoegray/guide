Search Bar

<%= form_tag subjects_path, :method => "get" do %>
  <p>
    <%= label_tag(:q, "Search for:") %>
    <%= text_field_tag(:q, params[:q]) %>
    <%= submit_tag("Search") %>
  </p>
<% end %>
